description: "AI PR Reviewer Evals"

prompts:
  - "Dummy prompt - custom provider uses its own templates"

providers:
  - id: file://custom-provider.ts
    label: "AI PR Reviewer"

tests: file://cases.yaml
